name: 桌面端构建与发布

# 推送 v* 标签 -> 发布 Release；推送 master 或手动运行 -> 仅构建，产物在 Artifacts

on:
  push:
    branches: [master]
    tags: ["v*"]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"

      - name: 安装依赖并打包
        run: |
          python -m pip install -r requirements.txt "pyinstaller>=6.0"
          pyinstaller --noconfirm yuanjisong.spec

      - name: 压缩产物
        shell: pwsh
        run: Compress-Archive -Path "dist\*" -DestinationPath "猿急送筛选系统-${{ github.ref_name }}.zip"

      - name: 上传产物（非 tag 触发时）
        if: ${{ !startsWith(github.ref, 'refs/tags/v') }}
        uses: actions/upload-artifact@v7
        with:
          name: desktop-windows
          path: 猿急送筛选系统-*.zip

      - name: 发布 Release
        if: startsWith(github.ref, 'refs/tags/v')
        shell: pwsh
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          $zip = Get-ChildItem -Filter "猿急送筛选系统-*.zip" | Select-Object -First 1
          gh release create "${{ github.ref_name }}" $zip.Name --title "${{ github.ref_name }}" --generate-notes
